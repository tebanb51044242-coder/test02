export interface User {
  name: string;
  branch: string;
  email: string;
  phone: string;
  registeredAt: string;
  birthDate: string;
  gender: string;
  goal: string;
  active: boolean;
  tag: string;
}